# Command-line Apps: First install 'app installer' from microsoft store manually
	# VLC
	winget install -e --id VideoLAN.VLC --accept-package-agreements
	# Breave Browser
	winget install -e --id BraveSoftware.BraveBrowser --accept-package-agreements
	# Adobe reader DC
	winget install -e --id Adobe.Acrobat.Reader.32-bit --accept-package-agreements
	# Powertoys
	winget install -e --id Microsoft.PowerToys --accept-package-agreements --accept-source-agreements
	# VMware Workstation
	winget install -e --id VMware.WorkstationPro --accept-package-agreements --accept-source-agreements
	# Gimp
	winget install -e --id GIMP.GIMP --accept-package-agreements --accept-source-agreements
	# Image glass
	winget install -e --id DuongDieuPhap.ImageGlass --accept-package-agreements --accept-source-agreements
	# Jdownloader
	winget install -e --id AppWork.JDownloader --accept-package-agreements --accept-source-agreements
	# Obsidian
	winget install -e --id Obsidian.Obsidian --accept-package-agreements --accept-source-agreements
	# RStudio
	winget install -e --id RStudio.RStudio.OpenSource --accept-package-agreements --accept-source-agreements
	# WinSCP
	winget install -e --id WinSCP.WinSCP --accept-package-agreements --accept-source-agreements
	# Advanced Files app
	winget install -e --id Files-Community.Files --accept-package-agreements --accept-source-agreements
	# Zotero
	winget install -e --id Zotero.Zotero --accept-package-agreements --accept-source-agreements
	# Discord
	winget install -e --id Discord.Discord --accept-package-agreements --accept-source-agreements
	# Teamviewer
	winget install -e --id TeamViewer.TeamViewer --accept-package-agreements --accept-source-agreements
	# Darktable (Adobe lightroom alternative)
	winget install -e --id darktable.darktable --accept-package-agreements --accept-source-agreements
	# Update all apps
	winget upgrade --all --accept-package-agreements --accept-source-agreements
# NOTE: you can create a '.bat' file with the commands for the softwares you want to install and just run the 'bat' file.
	

# Install from Appstore
	# Drawboard PDF
	# VSCode

# Optional Install from Appstore:
#	Adobe Photoshop Express
#	Journal
#	Amazon Appstore
#	Splash
#	FocusCommit
	
# Download Install
# 	R-Base
#	Lightroom
#	Jami (P-2-P video conferencing)
#	Postmaster (Advanced network control, per app DNS)
